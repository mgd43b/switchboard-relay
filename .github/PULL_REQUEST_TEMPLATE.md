## What & why

<!-- What does this change do, and what problem does it solve? -->

## Checklist

- [ ] `ruff check .` and `ruff format --check .` pass
- [ ] `pytest` passes with coverage ≥ 95%
- [ ] Tests added/updated in the right tier (`unit` / `feature` / `integration`)
- [ ] Docs updated (README / CHANGELOG) if behavior or tools changed
